[project]
name = "when-buy-bitcoin"
version = "0.1.0"
description = "Rules-based Bitcoin market-regime dashboard skill runtime"
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.24",
  "pandas>=2.0",
  "requests>=2.31",
]
